/**
 * Todo route paths extracted from route configuration
 * These match the paths defined in ./routes.ts
 */
export const todoPaths = {
  all: "/",
  active: "/active",
  completed: "/completed",
} as const
