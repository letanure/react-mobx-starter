/**
 * Static constants that never change at runtime
 * Only add constants that are actually used in the codebase
 */

// Example constants (uncomment and modify as needed)
// export const VALIDATION = {
//   TODO_MAX_LENGTH: 500,
//   PASSWORD_MIN_LENGTH: 8,
// } as const

// export const TIME = {
//   DEBOUNCE_DELAY: 300,
//   ANIMATION_DURATION: 200,
// } as const

// export const STORAGE_KEYS = {
//   USER_PREFERENCES: "user_preferences",
//   THEME: "theme",
// } as const

